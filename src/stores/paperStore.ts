import { defineStore } from "pinia";
const paperStore = defineStore("paperStore", {
  // 公用数据
  state: () => {
    return {};
  },
  // 公用方法
  actions: {},
  persist: true,
});
export default paperStore;
